{
  "name": "xhs-comments-replies",
  "platform": "xhs",
  "category": "replies",
  "version": "1",
  "doc": "sub-comment (reply) page of one top-level note comment; each reply carries a user_info author object whose profile fields the collector normalizes into model.UserProfile. Evidence: '/api/sns/web/v2/comment/sub/page' + noteSubCommentHandler strings in the original software (app-extracted/public/electron/_strings.js). Reply-target parameter name root_comment_id per the A-line corpus verdict (xhs_note_detail_comments: 64/64 sub/page requests use root_comment_id, zero bare comment_id; silent-scraping TODO-C).",
  "transport": {
    "base_url": "https://www.xiaohongshu.com",
    "path": "/api/sns/web/v2/comment/sub/page",
    "method": "GET",
    "placeholders": ["root_comment_id"]
  },
  "cookie": {
    "required": ["web_session"],
    "source": "browser profile import or env MEDIAMON_XHS_COOKIES"
  },
  "binding": {
    "comments": "$.data.comments",
    "fields": {
      "user_info.avatar_url": "$.user_info.image",
      "user_info.ip_label": "$.ip_location"
    }
  },
  "paging": {
    "cursor_param": "cursor",
    "count_default": 20,
    "has_more_path": "$.data.has_more",
    "next_cursor_path": "$.data.cursor"
  }
}
