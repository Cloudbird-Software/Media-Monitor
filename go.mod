module github.com/Cloudbird-Software/Media-Monitor

go 1.24

// Dependency policy: stdlib only (no external requires). Any new require is a
// C1 supply-chain change and must go through the org dependency approval
// process (proposal: name/purpose/license/stdlib_alternative), see docs/OPERATIONS.md.